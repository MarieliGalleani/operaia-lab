# Virtual Office — configuração de dados
#
# Caminho principal: API real da Equipe Digital.
# Mock apenas para testes/isolamento: VITE_USE_REAL_API=false

# false = mock local | qualquer outro valor (ou ausente) = API real
# VITE_USE_REAL_API=false

# Base URL da API Fastify (prefixo /api/v1).
VITE_API_URL=https://api.example.com/api/v1
