# Virtual Office — configuração de dados
# Por padrão a UI usa dados mockados. Para consumir a API real:

# Liga os adapters HTTP (createHttpGateways) no lugar dos mocks.
VITE_USE_REAL_API=false

# Base URL da API Fastify (prefixo /api/v1).
VITE_API_URL=http://localhost:3333/api/v1
